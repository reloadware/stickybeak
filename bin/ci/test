#!/usr/bin/env bash

set -o pipefail

cd /srv
mkdir -p test-results
pipenv run pytest --cov-config=.coveragerc --cov=stickybeak --junitxml=./test-results/summary.xml --cov-report=xml --cov-report=html
